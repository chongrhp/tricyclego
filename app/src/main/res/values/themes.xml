<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TricycleGo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- <item name="colorPrimary">#fff</item> -->
        <item name="colorPrimaryDark">@color/border_text</item>
        <item name="colorPrimary">@color/border_text</item>
        <item name="colorAccent">@color/border_text</item>
    </style>

    <style name="Theme.TricycleGo" parent="Base.Theme.TricycleGo" />
    <style name="splashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>